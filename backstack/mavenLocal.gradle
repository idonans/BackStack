apply plugin: 'maven'

group = 'com.idonans.backstack'
version = rootProject.ext.bcfg.ver.versionName

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri("../repositoryLocal"))
        }
    }
}